# Myrtille Games — Environment Variables
# Copy this file to .env.local and fill in the values
# DO NOT commit .env.local to git

# URL of the deployed Socket.IO server (Render)
# In development: http://localhost:3001
# In production: https://myrtille-games-server.onrender.com (update after Render deployment)
VITE_SERVER_URL=http://localhost:3001

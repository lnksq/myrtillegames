services:
  - type: web
    name: myrtille-games-server
    env: node
    region: frankfurt
    plan: free
    rootDir: apps/server
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
